{
  "name": "pulsewatch-monorepo",
  "version": "1.0.0",
  "description": "Groww Code 2026 Challenge - PulseWatch Smart Market Watchlist",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend install && cd backend && npx prisma db push && npx tsx src/db/seed.ts && npm run build",
    "start": "npm --prefix backend start",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "test": "npm --prefix backend run test"
  }
}
